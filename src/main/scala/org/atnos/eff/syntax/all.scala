package org.atnos.eff.syntax

object all extends all

trait all extends
  eval with
  option with
  disjunction with
  error with
  reader with
  writer with
  choose with
  list with
  state with
  eff

